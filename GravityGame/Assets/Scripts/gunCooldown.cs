using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gunCooldown : MonoBehaviour
{
    public bool canUseGunHead;
    // Start is called before the first frame update
    void Start()
    {
        canUseGunHead = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
